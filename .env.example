# Pulse frontend configuration
#
# Leave VITE_API_URL UNSET to run in MOCK mode (seeded data, no backend).
# Set it to your Express backend base URL to flip every api-client function
# from mock data to real `fetch` calls — no component changes required.
#
# VITE_API_URL=http://localhost:4000
